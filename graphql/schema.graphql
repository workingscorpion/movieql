type Movie {
  id: Int!
  name: String!
  score: Int!
}

type Query {
  movies: [Movie]!
  movie(id: Int!): Movie
  #String 뒤의 !는 isRequire을 의미
}

type Mutation {
  addMovie(name: String!, score: Int!): Movie!
}
